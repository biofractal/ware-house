.container
	h2 Houses
	table.table.table-bordered
		tr.info
			th ID
			th Client
			th Products
		tr(ng-repeat="house in houses track by house._id", ng-click="$state.go('root.house', {id:house._id})")
			td {{house._id}}
			td {{house.client}}
			td
				ul
					li(ng-repeat="_id in house.products") {{_id}}

